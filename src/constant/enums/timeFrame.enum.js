const timeFrameType = {
  DAILY: "DAILY",
  WEEKLY: "WEEKLY",
  MONTHLY: "MONTHLY",
  YEARLY: "YEARLY",
};
const timeFrameEnum = Object.keys(timeFrameType);

module.exports = {
  timeFrameType,
  timeFrameEnum,
};
