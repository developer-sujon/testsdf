const statsType = {
  NEW: "NEW",
  EXCLUSIVE: "EXCLUSIVE",
  PRO_BUYER: "PRO_BUYER",
  RATED: "RATED",
  VERIFY: "VERIFY",
};

const statsEnum = Object.keys(statsType);

module.exports = {
  statsType,
  statsEnum,
};
